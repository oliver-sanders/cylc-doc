name: test

on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: configure python
        uses: actions/setup-python@v1
        with:
          python-version: '3.7'

      - name: configure node
        uses: actions/setup-node@v1
        with:
            node-version: '10.x'

      - name: install graphviz
        run: |
          sudo apt-get update
          sudo apt-get install -y graphviz pkg-config libgraphviz-dev
          pip install pygraphviz

      - name: checkout cylc-flow
        uses: actions/checkout@v2
        with:
          reposiory: 'https://github.com/cylc/cylc-flow'
          path: 'cylc-flow'
      - name: install cylc-flow
        run: |
          (cd "$GITHUB_WORKSPACE/cylc-flow"; pip install -e .[all])

      - name: checkout cylc-doc
        uses: actions/checkout@v2
        with:
          path: 'cylc-doc'
      - name: install cylc-doc
        run: |
          (cd "$GITHUB_WORKSPACE/cylc-doc"; pip install -e .[all])

      - name: install eslint
        run: |
          npm install -g eslint

      - name: lint
        run: |
          pycodestyle .
          eslint .

      - name: build
        run: |
          make html slides linkcheck SPHINXOPTS='-Wn'

      - name: tutorial suite
        run: |
          mkdir -p "${HOME}/cylc-run"
          bin/run-validate

      - name: debug
        if: failure()
        run: |
          env 
          cat /tmp/sphinx-err*  # sphinx traceback
          find ~/cylc-run -name job.err -exec cat {} +  # cylc error files
